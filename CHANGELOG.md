# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-09-19

时域滤波与场景切换检测。**核心结论与最初的设想相反**，报告里按实测数据写。

### 时域

- **抖动源建模**：光源强度波动、闪烁相位漂移、**执行器量化**（曝光时间寄存器步进 /
  增益档位）。默认全关，关闭时数值与之前逐位相同
- **噪声地板实测**：480x360 下约 1.2e-4 EV —— 静止场景的 AE 抖动等于浮点噪声，
  「时域滤波把抖动降低 90%」在这个分辨率下是伪结论；扰动必须显式注入才有意义
- **对数域 EMA 时域滤波**：作用在测光量上（不是 EV 指令），过曝帧旁路，
  误差大时自动切 fast。状态挂控制器实例，不挂配置对象
- **场景切换检测**：三个逐帧信号投票（曝光归一化测光量 / 结构信号 / 直方图距离）。
  静止 10 组条件**零误触发**（含「让 AE 从 +3 EV 收敛」这条最要命的假阳性），
  切换事件检出延迟 0 帧，分类准确率 1.00
- **AWB 时域稳定**：对数域滤波 + 光源角度门控，增益跳动降 5.8 倍且不引入偏差

### 三个和直觉相反的实测结论

- 时域滤波在 AE **闭环**里不降抖动，开太狠反而失稳（alpha<=0.2 时 0.21 EV，0.1 时发散）。
  根因是变步长控制律的大环路增益叠加滤波的相位滞后。**滤波强度存在稳定边界**
- 真正有价值的是检测器：同一 alpha 下重收敛 17 帧 → **6 帧，而抖动分毫不变**
- AWB 侧结论相反，因为它是**开环估计器** —— 同一种滤波，放在开环里是纯收益，
  放进闭环里得先算稳定边界

### 已知边界

- 等亮度色温切换（5000K→3000K）检出 **0/2**：三个信号测的是亮度与内容，不测色度
- 抖动源幅度是仿真里设定的，不是真机实测谱

### 工程

- AE 新增逐帧流式接口 `run_stream()`，与 `run()` 共用同一份控制律（`_observe()`）；
  既有结论经整份报告 diff 验证**零变化**（除生成时间外无差异）
- 测试 28 → **38** 个，新增的 10 个全部是**性质测试**，其中一条把
  「噪声地板极低」这个负结论固化下来，防止以后拿假改善去写报告
- 实验 11 → **15** 组；全量运行 70s → 约 4 分钟（时域实验是逐帧序列，占大头）

## [0.1.0] - 2026-09-19

首个公开版本：从物理建模到量化评价的完整 3A 实验平台。

### 仿真链路

- 光学：抗锯齿离焦 PSF（子采样覆盖面积）、运动模糊、镜头阴影（分通道）、横向色差
- 传感器：满阱、光子散粒噪声、读出噪声、暗电流、CFA 光谱串扰、ADC 量化
- 光源：黑体色温（普朗克光谱 × CIE 1931 CMF）、交流纹波带纹
- 场景：标准色卡 / 对焦标板 / 逆光人像 / 大面积单色 / 匀光板，均带真值

### ISP

- BLC → LSC（RAW 域分 CFA 通道）→ 去马赛克（双线性 / 色差插值）
  → WB → CCM（色卡最小二乘标定）→ 色调映射（肩部压缩 + S 曲线）→ 降噪锐化

### 3A

- AE：5 种测光方式（平均 / 中心 / 点 / 分区评价 / 高光优先）、log 域变步长闭环、
  过曝比例反馈、抗闪烁曝光约束、曝光时间与增益分配策略、标定查表
- AWB：灰世界 / 白块 / 灰边 / Shades-of-Gray / 置信度加权融合、
  CCT-Duv 分解、普朗克轨迹约束
- AF：Brenner / Tenengrad / 拉普拉斯方差 / SML / FFT 高频占比 5 种评价函数，
  全扫描 / 爬山 / 粗到细 / 黄金分割 4 种搜索策略

### 评价

- 光源角度误差、逐块 ΔE00、中性区残余色度、CCT/Duv
- AE：收敛帧数、目标码值误差、过曝比例、方向反转次数
- AF：峰位误差、半高宽、动态范围、单调性破坏次数、峰位抖动
- ISP：亮度均匀度、PSNR/SSIM

### 画质指标（v0.1.0 追加）

- 斜边法 MTF（ISO 12233）：平顶 Tukey 窗 + 按 5σ 自适应定窗，
  七个已知模糊量下 MTF50 偏差 ≤ 2.8%，整条曲线 ≤ 0.022
- 光子转换曲线（EMVA 1288）：加权最小二乘 + 饱和点剔除，
  转换增益反推偏差 +0.9%，读出噪声偏差可由量化噪声解释
- 动态范围：满阱/读出噪声口径，与理论值一致
- 阴影：亮度均匀度 + 色阴影 Δu'v'，覆盖 LSC 欠校正 / 正确 / 过校正
- 传感器新增读出噪声参考位置模型（ISO 无关 / 读出噪声后置）
- 测试从 21 个增至 28 个，新增的 7 个全部用于验证测量能与真值对上

### 工程

- src-layout 包结构，`pip install -e .` 后提供 `aaa-isp-lab` 命令
- 28 个单元测试（含 CIEDE2000 官方测试数据），GitHub Actions 跨 3.9/3.10/3.11
- 11 组实验一键复现（约 1000 次成像），生成自包含 HTML / Markdown 报告
